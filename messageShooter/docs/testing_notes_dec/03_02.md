# feature/async_queue
let's make it rain!!!!!
I want to convert queue processor into async calls so that I can run mULTIPLE queues at the same time if desired
since 1 queue = 1 token, we can move forward

### Queue
- [X] Async function to run multiple queues at the same time
- [X] Check rate limiting by adding sleep time ("breath")
- [X] Change Queue Processor to be able to receive BIG lists of target lists
- [X] Wrapper function on Django to run async functions
- [X] Update Admin for manually Processing Queues

NEXT:
when i delete a target list, the queue is being deleted. Why's that?
Add 3 contacts at each list and process queue with 2 lists to see if it works as expected
4 pending tests to be fixed